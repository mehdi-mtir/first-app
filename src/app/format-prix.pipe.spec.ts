import { FormatPrixPipe } from './format-prix.pipe';

describe('FormatPrixPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPrixPipe();
    expect(pipe).toBeTruthy();
  });
});
